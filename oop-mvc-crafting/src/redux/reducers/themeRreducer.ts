// import { CHANGE_THEME, ENABLE_BUTTONS, DISABLE_BUTTONS } from '../types';

// const initialState = {
//   value: 'light',
//   disabled: false,
// };

// export default function themeReducer(state = initialState, action: any) {
//   switch (action.type) {
//     case CHANGE_THEME:
//       return { ...state, value: action.payload };
//     case ENABLE_BUTTONS:
//       return { ...state, disabled: false };
//     case DISABLE_BUTTONS:
//       return { ...state, disabled: true };

//     default:
//       return state;
//   }
// }
